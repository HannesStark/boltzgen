# Protocol: nanobody-anything
#
# ==============================================================================
# SET A: SO4 / LIGAND-BLOCKING POCKET  (mouse MARCO SRCR)
# ==============================================================================
#
# --- Scientific rationale ---
# The SO4²⁻ binding pocket in 2OYA defines the canonical ligand-binding crevice
# of the MARCO SRCR domain. Nanobodies targeting this site can block receptor
# interactions with ligands (LDL, oxLDL, bacteria, etc.).
#
# NOTE: These positions are DIFFERENT from the original hotspot set (mouse_marco_
# nanobody_hotspot.yaml). The original hotspots use 2OYA label_seq 6,8,15,44,50,
# 52,72,83. This Set A uses Q9UEW3-based positions 429,431,438,467,473,475,495,506
# (= 2OYA label_seq 12,14,21,50,56,58,78,89), which is a related but distinct set
# of surface residues in the same functional region.
#
# --- Numbering note ---
# boltzgen uses mmCIF label_seq_id. The mouse_marco_srcr.cif file uses Q9UEW3
# numbering directly (seqid = Q9UEW3 position, starting at 417).
# The YAML binding field uses label_seq values; for this file, label_seq N =
# Q9UEW3 position N (since seqid = Q9UEW3 position, not sequential 1-N).
#
# Confirmed: Q9UEW3 label_seq mapping:
#   label_seq 12 = Q9UEW3 429,  label_seq 14 = Q9UEW3 431,
#   label_seq 21 = Q9UEW3 438,  label_seq 50 = Q9UEW3 467,
#   label_seq 56 = Q9UEW3 473,  label_seq 58 = Q9UEW3 475,
#   label_seq 78 = Q9UEW3 495,  label_seq 89 = Q9UEW3 506
#
# --- Verified residues (from mouse_marco_srcr.cif, Q9UEW3 numbering) ---
#   Q9UEW3 429 = THR  (label_seq 12)
#   Q9UEW3 431 = ARG  (label_seq 14)
#   Q9UEW3 438 = TYR  (label_seq 21)
#   Q9UEW3 467 = GLY  (label_seq 50)
#   Q9UEW3 473 = TYR  (label_seq 56)
#   Q9UEW3 475 = GLY  (label_seq 58)
#   Q9UEW3 495 = TRP  (label_seq 78)
#   Q9UEW3 506 = ASN  (label_seq 89)
#
# --- Diffusion parameters ---
# Recommended: NUM_STEPS=200, GUIDANCE_SCALE=0.1-0.3, TEMPERATURE=0.9-1.1
#
# --- Scaffold chain notes ---
# Each scaffold YAML already specifies its VHH chain via `include: - chain: id: B|A`.
# All scaffolds contain only a single VHH heavy chain (no light chain).
#
# --- Cys in CDRs ---
# By default nanobody-anything avoids Cys. To permit Cys in designed CDRs:
#   export EXTRA_ARGS='--inverse_fold_avoid ""' ./runs/run_nanobody_campaign.sh ...

entities:
  - file:
      path: ../targets/mouse_marco_srcr.cif
      include:
        - chain:
            id: A
      binding_types:
        - chain:
            id: A
            # Q9UEW3 positions verified from structure:
            # 429(THR), 431(ARG), 438(TYR), 467(GLY),
            # 473(TYR), 475(GLY), 495(TRP), 506(ASN)
            binding: 12,14,21,50,56,58,78,89

  - file:
      path:
        # MARCO SRCR has a deep, narrow ligand-binding crevice.
        # Best scaffolds for deep-pocket penetration:
        #   7EOW (21aa CDR3) — vWF A1 binding, maximum reach. RECOMMENDED.
        #   8COH (19aa CDR3) — high shape complementarity vs C4.
        - ../../example/nanobody_scaffolds/7eow.yaml
        - ../../example/nanobody_scaffolds/8coh.yaml